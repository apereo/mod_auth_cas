DESTDIR?=`@APXS@ -q LIBEXECDIR`
all:
	@APXS@ -c src/mod_auth_cas.c

install:
	if test ! -d ${DESTDIR}; then mkdir -p ${DESTDIR}; fi
	@APXS@ -i -S LIBEXECDIR=${DESTDIR} src/mod_auth_cas.la

clean:
	rm -rf src/mod_auth_cas.slo src/mod_auth_cas.la src/mod_auth_cas.lo src/.libs
